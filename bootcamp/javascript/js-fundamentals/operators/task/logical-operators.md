# Logical operators

1. What is the output of the following code?

```javascript
console.log('Apple' && 'Orange' || 'Mango');
console.log('Apple' || 'Orange' || 'Mango');
console.log(1 && 0 && 1);
```
